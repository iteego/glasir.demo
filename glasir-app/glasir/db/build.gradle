/* This file is renamed to build.gradle and added to the
   released zip package during the build process for glasir.db. */

// Make sure the enclosing build does not touch this project
tasks.each { it.enabled = false }
